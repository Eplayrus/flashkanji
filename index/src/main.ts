// Vite entry placeholder for the next build step.
// The current PWA runtime is shipped as static ES2023 in ../script.js so it can run without a build.
// Domain services under src/ are already typed and can be wired here when the UI is split into modules.
export {};
