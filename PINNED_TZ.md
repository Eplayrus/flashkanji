# Flash Kanji pinned TZ

## Задача
Исправить интеграцию Yandex Metrika для SPA с hash-маршрутизацией, чтобы переходы между экранами учитывались как отдельные просмотры страниц.

## Что сейчас важно
- приложение работает как SPA и меняет экраны через `location.hash`;
- в основной статистике Metrika сейчас видны в основном только `/index/` и `/recovery/entry.html`;
- нужно сохранить текущие цели и не сломать SPA-переходы.

## Что нужно сделать
- найти все актуальные точки инициализации счётчика с ID `109492033`;
- добавить `trackHash: true` во всех реально используемых входных HTML;
- не включать одновременно `trackHash: true` и ручной `hit` на каждый `hashchange`;
- не трогать существующие цели `study_btn` и `Keep`;
- не переводить приложение на History API и не переписывать маршрутную систему.

## Что проверить
- `index/index.html`;
- `recovery/entry.html`;
- `recovery/index.html`;
- `index/assets/index/index.html`;
- поиск по `ym(109492033`, `webvisor:true`, `accurateTrackBounce`, `mc.yandex.ru/metrika`, `ym(..., "hit"`;
- отдельные просмотры для `#home`, `#learn`, `#textbooks`, `#review`, `#stats`, `#achievements`, `#eva-room`, `#kanji`, `#jlpt-lesson`.

## Критерий готовности
- hash-переходы фиксируются как отдельные просмотры;
- двойного учёта нет;
- цели и текущая работа приложения продолжают работать как раньше.
